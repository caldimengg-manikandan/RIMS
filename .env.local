frontend(.env)

# Development Environment Settings
# Points to the local backend port (10000 since Render defaults to this)
NEXT_PUBLIC_API_BASE_URL=http://localhost:10000

# When deploying, this variable should be set in Render to your backend's URL:
# NEXT_PUBLIC_API_BASE_URL=https://your-backend-service.onrender.com
